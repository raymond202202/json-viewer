# Changelog

## v1.1.0 (2026-07-29)

### 新增功能
- **文件导入**：支持打开本地 `.json` / `.har` / `.txt` / `.jsonc` 文件
  - 工具栏「📂 打开文件」按钮
  - `Ctrl+O` 快捷键
  - 拖拽文件到窗口直接打开
- **HAR 文件识别**：自动识别 HAR 格式，显示请求数、域名统计
- **命令行支持**：支持 `JSON阅读器.exe myfile.json` 或双击关联文件打开
- **加载动画**：导入文件时左侧面板显示转圈 spinner +「导入中…」，右侧同步清空旧内容

### 改进
- **搜索增强**：
  - 搜索时自动展开包含匹配项的 JSON 块，折叠无关部分
  - 右侧面板收起时触发搜索自动展开右侧
  - 清空搜索恢复全部展开状态
- **Electron 架构优化**：
  - 新增 `preload.js` 桥接渲染进程与主进程
  - 支持单实例模式（避免多开）
  - 支持 macOS `open-file` 事件和 Windows 文件关联

### 文件变更
| 文件 | 说明 |
|------|------|
| `index.html` | 新增文件导入 UI、拖拽处理、HAR 检测、搜索增强 |
| `preload.js` | **新建** - contextBridge 暴露文件读取 API |
| `main.js` | 重写 - 命令行参数解析、单实例锁、IPC 处理 |
| `README.md` | 更新功能列表和项目结构 |

---

## v1.0.0 (2026-07-08)

### 初始版本
- **自动解析**：粘贴 JSON 实时渲染树形结构
- **展开/收拢**：点击节点折叠或展开
- **格式化/压缩**：美化缩进或压缩为一行
- **搜索**：搜索键名或值，上下导航
- **复制**：复制键值对或整块 JSON
- **花括号匹配**：点击树节点定位 JSON 块
- **面板折叠**：左右面板可独立收起
- **主题切换**：浅色/深色一键切换
- **语法高亮**：字符串/数字/布尔/null/键名分色显示

### 技术栈
- 前端：纯 HTML/CSS/JS，零框架依赖
- 桌面壳：Electron 33
- 打包：electron-builder（可选）
